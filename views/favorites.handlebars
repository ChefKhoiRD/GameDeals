<div class="flex flex-col items-center justify-center">
    
    <img src="{{game.thumb}}"
    class="p-1 bg-yellow-300 border rounded max-w-sm"
    alt="thumbnail for game">
    
    <div class="pb-5 text-5xl">
        <a href="/search/{{game.id}}">{{game.title}}</a>
    </div>
    <div>
        <button data-id="{{game.id}}" type='submint' class="bg-red-400 hover:bg-red-600 text-white font-bold py-2 px-4 rounded">DELETE</button>
    </div>
</div>


<script src="./js/search.js"></script>